from models.Card import Card


class Player:
    # def __init__(self, stack: int=0) -> None:
    #     self.stack = stack
    #     self.stack_committed = 0
    #     self.current_bet = 0
    #     self.is_in_game = False
    #     self.hole_cards = []
    # 2 cards, stack, stack_committed, has_folded, current_bet

    # table - 5 cards, player order, player turn, pot size, blinds, min_raise
    pass